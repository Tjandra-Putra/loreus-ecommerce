import React from 'react';
import { Container, Row, Col, Card, Button } from 'react-bootstrap';

const productDetail = (props) => {
	return (
		<Container>
			<h1>DETAIL VIEW TEST</h1>
		</Container>
	);
};

export default productDetail;
