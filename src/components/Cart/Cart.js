import React from 'react';
import { Container, Row, Col, Card, Button } from 'react-bootstrap';

const cart = (props) => {
	return (
		<Container>
			<h1>CART</h1>
		</Container>
	);
};

export default cart;
